"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export default function AdminLogin(){const[u,setU]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const router=useRouter();async function submit(x:React.FormEvent){x.preventDefault();setE("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:u,password:p})});const d=await r.json();if(!r.ok)return setE(d.error);router.replace("/admin");router.refresh()}return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><div className="w-full max-w-md rounded-3xl border border-amber-500/20 bg-[#0d0d0d] p-8"><p className="text-xs uppercase tracking-widest text-amber-500">Restricted Terminal</p><h1 className="luxury mt-2 text-3xl">Admin Command Login</h1><form onSubmit={submit} className="mt-7 space-y-4"><input required value={u} onChange={e=>setU(e.target.value)} placeholder="Operator username" className="w-full rounded-xl border border-white/10 bg-black px-4 py-3"/><input required value={p} onChange={e=>setP(e.target.value)} type="password" placeholder="Security phrase" className="w-full rounded-xl border border-white/10 bg-black px-4 py-3"/>{e&&<p className="text-sm text-red-400">{e}</p>}<button className="w-full rounded-xl bg-amber-500 py-3 font-bold text-black">Authenticate</button></form></div></main>}
