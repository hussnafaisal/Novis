"use client";
import{useState}from"react";
export default function Forgot(){const[e,setE]=useState("");const[m,setM]=useState("");async function submit(x:React.FormEvent){x.preventDefault();if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e))return setM("Enter a valid email address.");const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:e})});if(r.ok)setM("If the account exists, reset instructions have been requested.");}return <main className="grid min-h-[calc(100vh-64px)] place-items-center px-4"><div className="w-full max-w-md rounded-3xl border border-amber-500/20 bg-[#111] p-7"><p className="text-xs uppercase tracking-widest text-amber-500">Recovery</p><h1 className="luxury mb-7 mt-2 text-3xl">Forgot Password</h1><form onSubmit={submit} className="space-y-4"><input required type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="Email address" className="w-full rounded-xl border border-white/10 bg-black px-4 py-3"/>{m&&<p className="text-sm text-zinc-300">{m}</p>}<button className="w-full rounded-xl bg-amber-500 py-3 font-bold text-black">Request Reset</button></form></div></main>}
